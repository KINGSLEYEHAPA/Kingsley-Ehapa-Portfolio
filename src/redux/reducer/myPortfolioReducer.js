const portfolioData = {};

const myPortfolioReducer = (state = portfolioData, action) => {
  const { type, payload } = action;

  switch (type) {
    default:
      return state;
  }
};

export default myPortfolioReducer;
